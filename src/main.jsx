import React from 'react';
import ReactDOM from 'react-dom/client';
import MaverickOS from './App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MaverickOS />
  </React.StrictMode>
);
